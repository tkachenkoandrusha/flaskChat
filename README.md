# 🟦 Чат-проект на Flask + Socket.IO

Цей проект реалізує сучасний веб-чат з кімнатами, аватарами, бульбашками повідомлень та історією чатів.  
Використовуються: **Flask**, **Flask-SocketIO**, **SQLite**, **SQLAlchemy**, сучасний CSS для бульбашок.

---

## 📂 Структура проекту

chat-project/
│
├─ app.py # Flask сервер + Socket.IO
│ ├─ Обробляє HTTP-запити
│ └─ Відправляє події Socket.IO
│
├─ models.py # База даних SQLite через SQLAlchemy
│ ├─ User # Таблиця користувачів
│ ├─ ChatRoom # Таблиця кімнат
│ └─ RoomUser # Зв’язок користувачів з кімнатами
│
├─ templates/
│ └─ chat.html # HTML-шаблон
│ ├─ Сайдбар: кімнати + учасники
│ ├─ Основний чат
│ └─ Поле вводу повідомлень
│
├─ static/
│ ├─ style.css # Стилі (сучасні бульбашки, аватари)
│ └─ (додаткові JS/картинки)
│
└─ README.md # Документація



---

## ⚡ Потоки подій Socket.IO

Клієнт <---- Socket.IO ----> Сервер (Flask)

Клієнт відправляє: Сервер обробляє:

join -----------------------> Додає користувача до кімнати
leave -----------------------> Видаляє користувача з кімнати
send_message -----------------------> Зберігає та транслює повідомлення
create_room ------------------------> Створює нову кімнату
delete_room ------------------------> Видаляє кімнату з БД

Сервер відправляє: Клієнт обробляє:

message <----------------------- Додає повідомлення у чат (бульбашки)
chat_history<----------------------- Історія повідомлень кімнати
update_users<----------------------- Оновлює список учасників
room_created<---------------------- Додає нову кімнату у список
room_deleted<---------------------- Видаляє кімнату зі списку



---

## 🔹 Пояснення подій

- **join / leave** – обробка входу та виходу користувача з кімнати.
- **send_message** – надсилає текст повідомлення, сервер транслює всім у кімнаті.
- **chat_history** – надсилає історію повідомлень кімнати після приєднання користувача.
- **update_users** – оновлює список учасників, аватари та кольори користувачів.
- **room_created / room_deleted** – динамічне додавання чи видалення кімнат у всіх клієнтів.

---

## 🚀 Запуск проекту

1. Клонувати репозиторій:
```bash
git clone <URL_репозиторію>
cd chat-project
Створити та активувати віртуальне середовище:

bash
Копировать код
python3 -m venv venv
source venv/bin/activate      # Mac/Linux
venv\Scripts\activate         # Windows
Встановити залежності:


pip install -r requirements.txt
Запустити сервер:


python app.py
Відкрити браузер та перейти на:



http://localhost:5000
🎨 Особливості інтерфейсу
Сучасні бульбашки повідомлень (праворуч – свої, ліворуч – інших)

Кольорові аватари з ініціалом користувача

Динамічне додавання та видалення кімнат

Автоматичне скролювання чату вниз при новому повідомленні

Кожен користувач має власний колір повідомлень

📌 Подальші покращення
Зберігання історії повідомлень у SQLite з можливістю завантаження при вході

Додати приватні повідомлення між користувачами

Реалізувати темну/світлу тему

Покращити UX: звуки сповіщень, таймштампи у бульбашках, реакції на повідомлення

💻 Технології
Python 3.13+, Flask, Flask-SocketIO

SQLite + SQLAlchemy

HTML5, CSS3 (сучасний стиль бульбашок)

JavaScript ES6+ (динамічний чат, WebSocket події)